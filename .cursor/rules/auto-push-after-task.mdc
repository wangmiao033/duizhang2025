---
description: 在本仓库完成实质性代码改动后自动提交并推送
alwaysApply: true
---

# 完成后自动推送（duizhang2025）

当在本仓库**完成用户要求的代码/配置改动**且改动已保存时：

1. 若项目存在脚本，先执行 `npm run build`；若存在 `npm test`，一并执行并通过后再推送。
2. 将本次任务相关的变更 `git add`（不要纳入明显无关或临时文件；`scripts/` 下一次性补丁脚本除非用户要求否则勿提交）。
3. 使用一句清晰的中文或英文提交说明执行 `git commit`。
4. 对当前分支执行 `git push`（默认 `origin` 与当前跟踪分支；勿 `force push`）。

**跳过推送的情况**：用户明确说不要提交/不要推送；仅有只读问答、无文件改动；推送失败且需用户处理认证/冲突时，说明原因即可。
